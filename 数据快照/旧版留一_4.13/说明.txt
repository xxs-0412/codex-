旧版留一_4.13 数据快照说明
============================================================

用途
- 保存旧版留一验证阶段对应的数据快照，供历史结果复盘和方案追溯使用。

当前内容
- 处理后数据目录: `处理后数据`
- run 清单: `run清单.csv`

说明
- 该快照来自当前根目录下兼容保留的 `磨损数据（改）`。
- 当前已识别到 13 个 `Run*.csv` 文件。
- 旧版原始数据未在本轮整理中单独补存到 `原始数据`，因此该目录先保留为空。

使用约定
- 该快照只用于追溯旧版实验，不再作为后续新实验的默认数据源。
- 如果以后需要完全脱离根目录下的 `磨损数据（改）`，可以再补做一次原始/处理后对齐整理。
